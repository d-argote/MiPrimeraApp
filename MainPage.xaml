<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MiPrimeraApp.MainPage"
             BackgroundColor="White">
    <VerticalStackLayout Padding="20"
            VerticalOptions="Center"
            Spacing="20">
        <Label Text="¡Bienvenid@ PRUEBA!"
                FontSize="32"
                HorizontalOptions="Center"
                TextColor="Black"/>
        <Label x:Name="saludoLabel"
                Text="Escribe tu nombre y presiona el botón"
                FontSize="16"
                HorizontalOptions="Center"
                TextColor="Black"/>
        <Entry x:Name="nombreEntry"
                Placeholder="Escribe tu nombre aquí"
                FontSize="16"
                BackgroundColor="#EEEEEE"
                TextColor="Black"/>
        <Button x:Name="saludarBtn"
                Text="Saludar"
                Clicked="OnSaludarClicked"
                HorizontalOptions="Center"
                BackgroundColor="#512BD4"
                TextColor="White"/>

        <!-- Botón para ir a segunda pantalla - Lista de Frutas -->
        <BoxView HeightRequest="1" Color="#E0E0E0" Margin="0,10"/>
        <Button Text="🍓 Ver Lista de Frutas"
                Clicked="OnVerFrutasClicked"
                HorizontalOptions="Center"
                BackgroundColor="#FF6B6B"
                TextColor="White"
                CornerRadius="20"
                Padding="24,12"/>

        <Label Text="Waydroid OK"
                FontSize="12"
                HorizontalOptions="Center"
                TextColor="Green"/>
    </VerticalStackLayout>
</ContentPage>
